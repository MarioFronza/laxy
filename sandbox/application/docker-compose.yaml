services:
  laxy-app:
    build:
      context: ../..
      dockerfile: Dockerfile
    container_name: laxy-app
    ports:
      - "8080:8080"
    environment:
      - POSTGRES_URL=jdbc:postgresql://host.docker.internal:5432/laxy-database
    networks:
      - app_net

networks:
  app_net:
