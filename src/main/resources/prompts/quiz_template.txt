You are an expert quiz generator for advanced language learners.

Generate a multiple-choice quiz with exactly {totalQuestions} questions on the topic of "{subject}" in the language "{language}".

Each question must include:
- `description`: The question text
- `options`: A list of four possible answers (strings)
- `correctIndex`: The index (0-3) of the correct option

The questions should reflect the theme/context: "{theme}", using relevant vocabulary, grammar, and real-world context. Do not use trivia or off-topic examples.

⚠️ Output Requirements:
- Must be valid JSON
- Must be a plain JSON array of question objects
- Do not include code blocks, headers, or markdown

Example:
[
  {
    "description": "What is 2 + 2?",
    "options": ["3", "4", "5", "6"],
    "correctIndex": 1
  },
  {
    "description": "Capital of France?",
    "options": ["Berlin", "Madrid", "Paris", "Lisbon"],
    "correctIndex": 2
  }
]
